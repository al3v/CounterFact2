[project]
name = "adl-sae"
version = "0.1.0"
description = "SAE analysis for paraphrase-sensitive factual recall"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
